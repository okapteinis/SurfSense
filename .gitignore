.flashrank_cache*
podcasts/
.ruff_cache/

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local
.env.production
.env.development
*.env

# SOPS secrets management
# Never commit unencrypted secrets
secrets.yaml
**/secrets.yaml
# Age private keys (keep only on VPS/local)
keys.txt
**/keys.txt

# LLM Configuration with API keys
**/config/global_llm_config.yaml
**/config/global_llm_config.yaml.backup*

# SSH keys (NEVER commit these)
*.pem
*.key
id_rsa*
id_ed25519*
*.ppk

# Secrets and credentials
secrets/
*.secret
config.secret.*
credentials.json

# Database backups and dumps
*.sql
*.dump

# Logs (contain sensitive info)
*.log
logs/
*.log.*

# Build artifacts
.next/
.turbo/
dist/
build/
out/
.cache/

# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
.venv/
env/
ENV/
pipenv/
.pytest_cache/

# Database files
*.db
*.sqlite
*.sqlite3

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Deployment backups (local only, not for git)
*-backup-*.tar.gz
backups/

# Temporary files
tmp/
temp/
*.tmp

# Uploads and temp files
uploads/

# Coverage reports
coverage/
.nyc_output/
*.lcov

# OS files
Thumbs.db
.DS_Store

# Backup files
*.backup
*.backup.*
.env.backup*
pyproject.toml.backup*
security_audit_baseline*.json
installed_before_update.txt

# Production configs (use templates instead)
nginx.conf
surfsense-*.service

# Script configuration
scripts/.config
sync-from-production.sh
MIGRATION_LOCAL_LLM.md
PR_DESCRIPTION.md
